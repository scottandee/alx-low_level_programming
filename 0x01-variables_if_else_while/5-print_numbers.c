#include <stdio.h>
/**
 * main - entry point function
 * Description: this is the entry point function
 * Return: this function returns a value if zero
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}

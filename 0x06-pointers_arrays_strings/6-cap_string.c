#include "main.h"

/**
* *cap_string - this capitalizes the first letter of every word
* 
* Return:
*/

char *cap_string(char *);
{
	int length = 0;

	while (s[length] != '\0')
	{
		length++;
	}
	for 90
}

#include <stdio.h>
/**
  * main -entry point function
  * void: no parameters are entered into this function
  * Description: This the entry point of our program
  * Return: the function will return a value of zero
  */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

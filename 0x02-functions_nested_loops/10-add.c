#include "main.h"
/**
 * add - adds integers
 * Description: adds two integers together
 * @a: this is ine if the arguments
 * @b: this is the second argument
 * Return: the addition of the integers
 **/
int add(int a, int b)
{
	return (a + b);
}
